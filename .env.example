# Copy to .env.local and fill in. Never commit .env.local.
# `pnpm setup:check` reports presence/absence only — it never prints values.

# Supabase — new API key format (sb_publishable_... / sb_secret_...)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_PROJECT_REF=
SUPABASE_DB_PASSWORD=

# ElevenLabs
ELEVENLABS_API_KEY=

# OpenAI
OPENAI_API_KEY=

# Tavily
TAVILY_API_KEY=

# Optional — real phone mode (NOT configured in this build; feature hidden in UI)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
